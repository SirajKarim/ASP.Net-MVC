@model OnlinePrintingSolution.Models.MaterialType

@{
    ViewBag.Title = "Add";
}

<h2>Add</h2>

@using (Html.BeginForm()) {
    @Html.ValidationSummary(true)

    <fieldset>
        <legend>MaterialType</legend>

        <div class="editor-label">
            @Html.LabelFor(model => model.MaterialTypId)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MaterialTypId)
            @Html.ValidationMessageFor(model => model.MaterialTypId)
        </div>

        <div class="editor-label">
            @Html.LabelFor(model => model.MaterialName)
        </div>
        <div class="editor-field">
            @Html.EditorFor(model => model.MaterialName)
            @Html.ValidationMessageFor(model => model.MaterialName)
        </div>

        <p>
            <input type="submit" value="Create" />
        </p>
    </fieldset>
}

<div>
    @Html.ActionLink("Back to List", "Index")
</div>

@section Scripts {
    @Scripts.Render("~/bundles/jqueryval")
}
